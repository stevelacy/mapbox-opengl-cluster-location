{
  "scripts": {
    "copy": "cp src/index.html .",
    "start": "npm run copy && budo src/app.js --live --dir=. -- -t babelify",
    "build": "browserify src/app.js -o app.js -t [ babelify --presets [ es2015 ] ]"
  },
  "devDependencies": {
    "append-hook": "^1.1.0",
    "babel-preset-es2015": "^6.13.2",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "budo": "^8.3.0",
    "mapbox-gl": "^0.21.0",
    "mercury": "^14.1.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
